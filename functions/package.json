{
  "name": "@agentics/incident-manager-agents",
  "version": "1.0.0",
  "description": "Unified Cloud Function entry point for incident-manager agents (escalation, post-mortem, hitl)",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean",
    "start": "node dist/index.js",
    "test": "node --test dist/**/*.test.js",
    "test:types": "tsc --noEmit",
    "deploy": "gcloud functions deploy incident-manager-agents --runtime nodejs20 --trigger-http --region us-central1 --project agentics-dev --entry-point handler --memory 512MB --timeout 60s --no-allow-unauthenticated"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@agentics/contracts": "file:vendor/agentics-contracts-1.2.0.tgz",
    "@agentics/escalation-agent": "file:vendor/agentics-escalation-agent-1.0.0.tgz",
    "@agentics/ruvector-client": "file:vendor/agentics-ruvector-client-1.0.0.tgz"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
